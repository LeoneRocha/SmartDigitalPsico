version: '3.7' 
services:
  web:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8081:8081"
    restart: always
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
